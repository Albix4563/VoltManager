<Window x:Class="VoltManager.SchedulePowerActionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tempo personalizzato" Height="320" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Background="{DynamicResource ThemeBackgroundBrush}"
        Foreground="{DynamicResource ThemeTextBrush}">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ThemeSurfaceElevatedBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource ThemePrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextBrush}" />
        </Style>
    </Window.Resources>

    <Grid Margin="20" Background="{DynamicResource ThemeBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Tempo personalizzato" FontSize="18" FontWeight="Bold"
                   Foreground="{DynamicResource ThemeTextBrush}" Margin="0,0,0,16" />

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock x:Name="HoursLabel" Text="Ore" Foreground="{DynamicResource ThemeMutedTextBrush}" FontSize="12" Margin="0,0,0,4" />
                <TextBox x:Name="HoursTextBox" Text="0" TextAlignment="Center" FontSize="16" Padding="8,6" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock x:Name="MinutesLabel" Text="Minuti" Foreground="{DynamicResource ThemeMutedTextBrush}" FontSize="12" Margin="0,0,0,4" />
                <TextBox x:Name="MinutesTextBox" Text="30" TextAlignment="Center" FontSize="16" Padding="8,6" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock x:Name="ActionLabel" Text="Azione" Foreground="{DynamicResource ThemeMutedTextBrush}" FontSize="12" Margin="0,0,0,4" />
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="ShutdownRadio" IsChecked="True" GroupName="Action" Margin="0,0,16,0" FontSize="14">
                    <RadioButton.Content>
                        <TextBlock x:Name="ShutdownText" Text="Spegni" VerticalAlignment="Center" />
                    </RadioButton.Content>
                </RadioButton>
                <RadioButton x:Name="SleepRadio" GroupName="Action" FontSize="14">
                    <RadioButton.Content>
                        <TextBlock x:Name="SleepText" Text="Sospendi" VerticalAlignment="Center" />
                    </RadioButton.Content>
                </RadioButton>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="3" Background="{DynamicResource ThemeSurfaceBrush}"
                BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1"
                CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="SummaryLabel" Text="Il PC verrà spento alle --:--"
                           Foreground="{DynamicResource ThemeTextBrush}" FontSize="14" TextWrapping="Wrap" />
                <TextBlock x:Name="ErrorLabel" Foreground="#FF6B6B" FontSize="12"
                           Visibility="Collapsed" Margin="0,4,0,0" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="CancelButton" Content="Annulla" Width="100" Height="36"
                    Click="CancelButton_Click"
                    Background="{DynamicResource ThemeSurfaceElevatedBrush}"
                    Foreground="{DynamicResource ThemeTextBrush}"
                    BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1"
                    Margin="0,0,12,0" />
            <Button x:Name="ConfirmButton" Content="Conferma" Width="100" Height="36"
                    Click="ConfirmButton_Click"
                    Background="{DynamicResource ThemePrimaryBrush}"
                    Foreground="{DynamicResource ThemeOnPrimaryBrush}"
                    BorderBrush="{DynamicResource ThemePrimaryBrush}" BorderThickness="1" />
        </StackPanel>
    </Grid>
</Window>
