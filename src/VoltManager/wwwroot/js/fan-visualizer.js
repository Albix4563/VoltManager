/*
 * Lazy entry point. The WebGL visualizer is loaded together with Fan Center by
 * fans.js only when the cooling view is actually opened. Keeping this file tiny
 * preserves the existing index.html script contract without parsing WebGL code at boot.
 */
